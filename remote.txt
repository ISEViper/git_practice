git remote add <name> <url>
원격 저장소를 추가한다.
name은 보통 origin을 사용

git push <name> <branch>
git push origin master
내 저장소의 변경 내역(commit)들을 원격에 push

git clone <url>
저장소를 복제해온다.

git pull <name> <branch>
git pull origin master
원격 저장소에 있는 내용을 가져온다.